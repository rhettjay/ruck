return {
  -- Never used, CMD+T sets up a new window in ghostty, no need for another buffer
  -- {
  --   'akinsho/toggleterm.nvim',
  --   version = '*',
  --   config = function()
  --     require("toggleterm").setup({})
  --   end,
  -- }
}
